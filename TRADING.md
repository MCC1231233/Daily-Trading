# 자동 주문 실행

스크리닝 리포트를 한국투자증권(KIS) Open API로 실제 주문에 옮긴다.
**기본값은 모의투자 + dry-run이다.** 실계좌로 옮기기 전에 아래 "언제 실계좌로
옮기는가"를 반드시 읽어라.

---

## 왜 KIS인가

증권사를 수수료로 고르면 축을 잘못 잡는 것이다. 왕복 비용의 구성은 이렇다.

| 항목 | 왕복 |
|---|---|
| 증권거래세 (매도) | **0.200%** |
| 수수료 (KIS, 편도 0.0036%) | 0.007% |
| 슬리피지 (보수적, 대형주) | 0.100% |
| **합계** | **≈ 0.31% / 일** |

수수료는 총비용의 **3% 미만**이다. NH PLUG(0.01%)와 KIS(0.0036%)의 차이는
왕복 0.013%p로 거래세 앞에서 무의미하다. 실제로 갈리는 축은 둘이다.

1. **REST냐 COM/OCX냐** — COM 계열(LS xingAPI, 대신 CREON)은 HTS가 켜진
   윈도우 머신이 매일 09:00에 살아 있어야 한다. REST는 리눅스 VM에서 돈다.
2. **모의투자 API 지원** — 실계좌 전에 무비용으로 검증할 수 있느냐.

KIS는 둘 다 만족하고 공식 파이썬 샘플 저장소가 있다.

> **tr_id 주의** — 웹에 널리 퍼진 `TTTC0802U`/`TTTC0801U`는 구버전이다.
> 현재는 `TTTC0012U`(매수)/`TTTC0011U`(매도)이고 `EXCG_ID_DVSN_CD` 필드가
> 추가됐다. 구버전 값으로 보내면 주문이 통째로 거부된다. 스펙을 고칠 일이
> 있으면 검색이 아니라 [koreainvestment/open-trading-api](https://github.com/koreainvestment/open-trading-api)를 봐라.

---

## 하루 흐름

| 시각 | 명령 | 하는 일 |
|---|---|---|
| 03:00 | (GitHub Actions) | 스크리닝 → `docs/data/YYYY-MM-DD.json` |
| 08:30 | `trade.py prepare` | 리포트가 없으면 스크리너를 직접 실행 |
| 08:50 | `trade.py entry` | 지정가 매수 접수 → 09:00 시가 체결 |
| 09:05 | `trade.py sweep` | 미체결 잔량 취소 |
| 15:20 | `trade.py exit` | 시장가 매도 접수 → 15:30 종가 체결 |
| 15:40 | `trade.py settle` | 실제 체결가로 세후 수익률 계산 |

### 시각을 바꾸지 마라

그림자 모드는 일봉의 **시가→종가**로 채점한다. 실행이 그와 다르면 측정한
수익률과 실제 수익률이 매일 어긋나고, 몇 달 쌓은 표본이 통째로 무의미해진다.

- **진입 08:50 지정가** → 장시작 동시호가(08:30~09:00) 참여 → 09:00 시가 체결
- **청산 15:20 시장가** → 종가 단일가(15:20~15:30) 참여 → 15:30 종가 체결

청산에만 시장가를 쓰는 이유는 단일가 매매에서 시장가가 가격 결정에 최우선
참여해 사실상 종가 체결을 보장하기 때문이다. 반대로 **진입에 시장가를 쓰면
장시작 동시호가에서 호가가 얇을 때 상한가까지 긁는다** — 절대 쓰지 마라.

"오후 3시 매도"로 알고 있었다면 여기서 정의가 한 번 조정된 것이다.
15:00에 팔면 채점 기준(종가)과 실행이 매일 어긋난다.

### GitHub Actions를 쓰지 않는 이유 — 그리고 `prepare` 가 필요한 이유

예약 워크플로 지연은 통제할 수 없다. 08:50 진입과 15:20 청산은 분 단위로
맞아야 하므로 애초에 Actions 로 할 수 없다.

문제는 스크리닝도 안전하지 않다는 것이다. 2026-09-08 에 `generated_at` 을
15일치 세어보니 이렇다.

| 매매일 | 생성 시각(KST) | 07:40 대비 |
|---|---|---|
| 08-19 | 07:55 | +15분 |
| 08-24 | 07:53 | +13분 |
| 08-31 | 09:48 | +128분 |
| 09-01 | 10:22 | +162분 |
| 09-02 | 09:29 | +109분 |
| 09-03 | 09:34 | +114분 |
| 09-04 | 09:19 | +99분 |
| 09-07 | 09:18 | +98분 |

**08-31 이후 예약 실행 6/6 일이 09:00 이후에 끝났다** — 장이 열린 뒤에
리포트가 나온 것이다. 그 상태로는 08:50 진입이 매일 빈손으로 끝난다.

대응은 둘이다.

1. cron 을 **03:00 KST** 로 당겼다. 파이프라인은 전일 종가만 쓰므로 전날
   밤에 돌려도 결과가 같다. 관측된 최대 지연(162분)이 그대로 나도 05:45 에
   끝나 08:50 까지 3시간이 남는다.
2. 그래도 못 미더우면 매매 서버가 **직접 만든다**. `trade.py prepare` 는
   리포트가 있으면 아무것도 하지 않고, 없으면 스크리너를 돌린다(2~4분).
   매일 08:30 에 걸어두면 Actions 가 실패한 날에도 매매가 이어진다.

---

## 설치

### 1. 모의투자 계좌와 앱키

1. 한국투자증권 홈 → 트레이딩 → 모의투자 → 국내주식 리그 신청
2. [KIS Developers](https://apiportal.koreainvestment.com) 로그인 → API 신청
   → **모의투자 앱키/앱시크릿** 발급 (실전 앱키와 별개다)

### 2. 자격증명

```bash
cp .env.example .env
```

`.env`에 값을 채운다. **이 파일은 `.gitignore`에 있다 — 절대 커밋하지 마라.**

```
KIS_ENV=demo
KIS_APP_KEY=...
KIS_APP_SECRET=...
KIS_ACCOUNT=12345678-01
```

### 3. 배관 점검

```bash
python scripts/selftest_trade.py
```

네트워크 없이 주문 로직만 검증한다 (호가단위, 시간대, 멱등성, 정산 산식).
33개 항목이 통과해야 한다.

```bash
python scripts/trade.py preflight
```

토큰 발급 → 잔고 조회 → 시세 조회까지 실제로 해본다. **주문은 내지 않는다.**

### 4. 실행 켜기

`config.yaml`:

```yaml
trading:
  enabled: true
  dry_run: true    # 며칠 로그를 보고 예상과 맞으면 false로
```

`dry_run: true`면 무엇을 낼지만 출력하고 주문 API를 호출하지 않는다.

---

## 스케줄러 등록

### 리눅스 VM (권장 — 오라클 클라우드 무료티어 등)

```
30 8  * * 1-5 cd /opt/Daily-Trading && git pull -q && . .env && python scripts/trade.py prepare >> live/trade.log 2>&1
50 8  * * 1-5 cd /opt/Daily-Trading && . .env && python scripts/trade.py entry  >> live/trade.log 2>&1
5  9  * * 1-5 cd /opt/Daily-Trading && . .env && python scripts/trade.py sweep  >> live/trade.log 2>&1
20 15 * * 1-5 cd /opt/Daily-Trading && . .env && python scripts/trade.py exit   >> live/trade.log 2>&1
40 15 * * 1-5 cd /opt/Daily-Trading && . .env && python scripts/trade.py settle >> live/trade.log 2>&1
```

서버 시간대가 KST인지 확인해라 (`timedatectl set-timezone Asia/Seoul`).

### 윈도우 작업 스케줄러

작업 4개를 만들고 각각 `python C:\...\Daily-Trading\scripts\trade.py entry` 형태로
등록한다. 트리거는 평일 08:50 / 09:05 / 15:20 / 15:40.

### 청산 실패 감시 — 이것만은 꼭

`exit`이 한 종목이라도 실패하면 **종료코드 1**을 낸다. 그 상태를 모르고 넘어가면
오버나잇 노출이 생기고, 그건 이 전략의 정의를 위반한다. 종료코드가 0이 아닐 때
알림이 오도록 걸어둬라. 다음 날 `entry`는 잔고가 남아 있으면 스스로 거부하므로
노출이 두 배가 되지는 않지만, 그날 매매는 통째로 건너뛴다.

---

## 안전장치

| 장치 | 동작 |
|---|---|
| `trading.enabled: false` | 어떤 명령이든 즉시 종료 (기본값) |
| `dry_run: true` | 주문 API를 호출하지 않음 (기본값) |
| `KIS_ENV=demo` | 모의투자 도메인 (기본값) |
| `KIS_ALLOW_REAL` | `real` 전환에 이 값이 `I_UNDERSTAND`여야 함 (이중잠금) |
| 리포트 날짜 대조 | `trade_date`가 오늘이 아니면 진입 거부 — 어제 후보를 사는 사고 방지 |
| 잔고 확인 | 보유 종목이 남아 있으면 진입 거부 |
| 저널 확인 | 같은 날 두 번째 진입 거부 |
| 시간대 가드 | 지정 시간대 밖에서는 실행 거부 (`--force`로 우회) |
| POST 무재시도 | 주문 통신 오류 시 재시도하지 않음 — 중복 주문이 실패보다 나쁘다 |
| `max_order_krw` | 종목당 주문금액 상한 |
| 거래정지 감지 | `halted` 종목 자동 제외 |

**청산(`exit`)은 저널이 아니라 실제 잔고를 진실로 삼는다.** 저널 파일이 깨져도
청산은 되어야 하기 때문이다. 그래서 `exit`은 몇 번을 돌려도 안전하다.

---

## 언제 실계좌로 옮기는가

**모의투자에서 세후 초과수익이 t>2로 유지되기 전에는 옮기지 마라.**

측정 시점(2026-09-04) 라이브 그림자 모드:

| 변형 | 채점 | 일평균 | 세후 | t |
|---|---|---|---|---|
| 우량 과매도 (발행본) | 11일 | +0.778% | +0.47% | 0.48 |
| 과매도 단일팩터 (대조군) | 25일 | +1.238% | +0.93% | 0.80 |

세후로도 플러스지만 **아직 우연과 구분되지 않는다.** 일간 초과수익 표준편차가
3.34%p라 25일 표본의 95% 신뢰구간은 `[-0.77%, +1.86%]`이고, 여기서 비용
0.31%를 빼면 `[-1.08%, +1.55%]` — **손실 구간을 포함한다.** 시스템이 계산한
필요 표본은 대조군 151일, 발행본 191일(약 8~9개월)이다.

여기에 이 저장소의 연구 결론이 겹친다 — 12년 생존편향 제거 패널 9,300설정에서
KOSPI를 t>2로 이긴 롱온리 전략은 0개였고, 데일리 매매 2,352설정에서도 검증
통과 0개였다. 지금의 누적 +35%는 25거래일치이고, 그 25일은 2025~2026 대형주
멜트업 국면이다.

### 모의투자 단계에서만 알 수 있는 것

백테스트와 그림자 모드는 "시가에 사서 종가에 판다"는 **가정**을 채점한다.
`settle`은 **실제 체결가**로 다시 계산한다. 둘의 차이가 슬리피지이고,
그건 어떤 백테스트로도 알 수 없다. 구체적으로 이것들이 처음 숫자로 나온다.

- 09:00 동시호가에서 10종목 동시 진입 시 실제로 얼마나 밀리는지
- 지정가 밴드 2%를 넘겨 갭상승해 못 산 날이 얼마나 되는지
- 종가 단일가 매도가 실제로 종가에 체결되는지
- 상한가·거래정지로 청산이 막히는 빈도

---

## 명령 요약

```bash
python scripts/selftest_trade.py           # 네트워크 없이 주문 로직 검증
python scripts/trade.py preflight          # 연결 점검 (주문 없음)
python scripts/trade.py status             # 계좌 현황
python scripts/trade.py prepare            # 리포트 없으면 스크리너 실행
python scripts/trade.py entry              # 진입
python scripts/trade.py sweep              # 미체결 취소
python scripts/trade.py exit               # 청산
python scripts/trade.py exit --force       # 시간대 무시하고 강제 정리
python scripts/trade.py settle             # 정산
python scripts/trade.py settle --date 2026-09-04   # 과거 날짜 재정산
```

`--dry-run`은 config와 무관하게 주문을 막는다.

---

## 체결 기록

`live/YYYY-MM-DD.json`에 남는다. **`.gitignore`에 있다** — 계좌 잔고와
주문번호가 담기는데 대시보드는 공개 저장소이기 때문이다.

`trading.publish_summary: true`로 두면 `docs/data/live_summary.json`에
**수익률만** 기록해 대시보드에 노출한다. 금액·수량·주문번호는 올리지 않는다.

---

## 법적 사항

본인 계좌를 본인이 만든 프로그램으로 매매하는 것은 합법이다.
타인의 자금을 운용하면 투자일임업 등록 대상이 된다.
